<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Match Care ABA – Start Your Free Match Now</title>
<link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700;800;900&family=Quicksand:wght@400;500;600;700&display=swap" rel="stylesheet">
<style>
  :root {
    --sky: #EEF6FF;
    --white: #FFFFFF;
    --navy: #1B3A6B;
    --navy-mid: #2A5298;
    --gold: #F5A623;
    --gold-light: #FFD166;
    --coral: #FF6B6B;
    --mint: #4ECDC4;
    --lavender: #A78BFA;
    --soft-green: #6BCB77;
    --text: #1B3A6B;
    --muted: #6B7FA3;
    --card-bg: #FFFFFF;
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }
  html { scroll-behavior: smooth; }

  body {
    font-family: 'Nunito', sans-serif;
    background: var(--sky);
    color: var(--text);
    overflow-x: hidden;
  }

  /* Navigation */
  nav {
    position: fixed; top: 0; left: 0; right: 0; z-index: 100;
    display: flex; align-items: center; justify-content: space-between;
    padding: 16px 60px;
    background: rgba(255,255,255,0.95);
    backdrop-filter: blur(12px);
    border-bottom: 2px solid rgba(245,166,35,0.2);
  }

  .nav-logo { display: flex; align-items: baseline; gap: 5px; text-decoration: none; }
  .logo-main { font-size: 1.45rem; font-weight: 900; color: var(--navy); }
  .logo-aba { font-size: 1.1rem; font-weight: 900; color: var(--gold); }

  /* Hero Section */
  .hero {
    min-height: 100vh;
    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: center;
    padding: 120px 60px 80px;
  }

  .hero-visual { display: flex; justify-content: center; }

  .hero-photo-wrap {
    width: 450px;
    height: 450px;
    border-radius: 50%;
    overflow: hidden;
    border: 10px solid white;
    box-shadow: 0 24px 60px rgba(27,58,107,0.2);
    position: relative;
  }

  .hero-photo {
    width: 100%;
    height: 100%;
    object-fit: cover;
    /* This zooms in significantly on the girl's face */
    transform: scale(2.1);
    object-position: center 15%;
  }

  /* Form Container */
  .form-section { padding: 90px 20px; background: var(--sky); }
  .lead-form {
    background: white;
    border-radius: 28px;
    padding: 40px;
    max-width: 550px;
    margin: 0 auto;
    box-shadow: 0 16px 50px rgba(27,58,107,0.1);
  }

  .phase-panel { display: none; }
  .phase-panel.active { display: block; animation: fadeIn 0.4s ease; }

  @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }

  .opt-btn {
    display: block; width: 100%; padding: 16px; margin-bottom: 12px;
    border-radius: 12px; border: 2px solid #e8edf5; background: #f8fafc;
    cursor: pointer; font-weight: 700; text-align: left; transition: 0.2s;
    font-size: 1rem; color: var(--navy);
  }
  .opt-btn:hover { border-color: var(--gold); background: #fffdf9; }

  .btn-primary {
    width: 100%; padding: 18px; border-radius: 50px; border: none;
    background: var(--navy); color: white; font-weight: 900; cursor: pointer;
    font-size: 1rem;
  }

  /* Responsive */
  @media (max-width: 900px) {
    .hero { grid-template-columns: 1fr; text-align: center; }
    .hero-photo-wrap { width: 300px; height: 300px; margin-top: 40px; }
  }
</style>
</head>
<body>

<nav>
  <a href="#" class="nav-logo">
    <span class="logo-main">Match Care</span><span class="logo-aba">ABA</span>
  </a>
</nav>

<section class="hero">
  <div class="hero-content">
    <h1 style="font-size: 3.2rem; line-height: 1.1; color: var(--navy); font-weight: 900;">Find the <span style="color:var(--gold)">Perfect</span><br>ABA Match</h1>
    <p style="margin: 25px 0; color: var(--muted); font-size: 1.1rem;">We help families find the best therapy for their unique needs.</p>
    <a href="#match-form" class="btn-primary" style="text-decoration:none; display:inline-block; width:auto; padding: 15px 35px;">Start Your Free Match</a>
  </div>
  <div class="hero-visual">
    <div class="hero-photo-wrap">
      <img src="https://images.unsplash.com/photo-1594631252845-29fc458639a6?ixlib=rb-1.2.1&auto=format&fit=crop&w=800&q=80" alt="Matching Care" class="hero-photo">
    </div>
  </div>
</section>

<section id="match-form" class="form-section">
  <div class="lead-form">
    <div id="step-1" class="phase-panel active">
      <h2 style="margin-bottom:20px;">Where are you located?</h2>
      <button class="opt-btn" onclick="goToStep('next')">New York</button>
      <button class="opt-btn" onclick="goToStep('next')">New Jersey</button>
      <button class="opt-btn" onclick="goToStep('other')">Another Location</button>
    </div>

    <div id="step-next" class="phase-panel">
      <h2>Step 2: Tell us more</h2>
      <p style="margin-bottom: 20px;">We're processing your request...</p>
      <button class="btn-primary" onclick="location.reload()">Continue</button>
    </div>

    <div id="step-other" class="phase-panel">
      <div style="text-align:center;">
        <span style="font-size: 3rem;">🌟</span>
        <h2 style="margin: 15px 0;">We're on it!</h2>
        <p style="color: var(--muted); line-height: 1.6; margin-bottom: 25px;">
          While we're still expanding in that area, <strong>we will get right on it for you.</strong>
          Our team will check our network and see if we can find a perfect match for your family!
        </p>
        <button class="btn-primary" onclick="location.reload()">Back to Home</button>
      </div>
    </div>
  </div>
</section>

<script>
  function goToStep(stepId) {
    document.querySelectorAll('.phase-panel').forEach(p => p.classList.remove('active'));
    if(stepId === 'other') {
      document.getElementById('step-other').classList.add('active');
    } else {
      document.getElementById('step-next').classList.add('active');
    }
  }
</script>

</body>
</html>
